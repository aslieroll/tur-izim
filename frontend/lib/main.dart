import 'package:flutter/material.dart';

import 'app/app.dart';

/// Tur İzim — giriş noktası (MVP Flutter kabuğu).
void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const TurIzimApp());
}
